import * as React from "react";
declare type Props = {
    children: React.ReactNode;
    parentElement?: HTMLElement;
};
declare function Layer({ children, parentElement }: Props): JSX.Element | null;
export default Layer;
